@model Dierentuin.Models.Category

@{
    ViewData["Title"] = "Create";
}

<div class="category-create-container">
    <h1 class="category-create-title">Create Category</h1>

    <div class="category-create-card">
        <div class="category-create-card-body">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Name" class="control-label"></label>
                    <input asp-for="Name" class="form-control" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Save" class="btn category-create-btn" />
                </div>
            </form>
        </div>
    </div>

    <div class="category-create-back-to-list">
        <a asp-action="Index">Back to List</a>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
