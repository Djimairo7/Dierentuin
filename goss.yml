#This file defines the Goss configuration for testing an ASP.NET application
#Define a process that should be running
process:
  dotnet:
    running: true

#Define user that should exist
user:
  www-data:
    exists: true

#Define DNS resolution
dns:
  localhost:
    resolvable: true